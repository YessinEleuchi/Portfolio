<section class="px-7 py-20 lg:py-28">
  <div class="mx-auto max-w-5xl">

    <!-- HEADER -->
    <div class="mb-10">
      <p
        class="mb-2 text-[10px] font-medium uppercase
               tracking-[0.3em] text-neutral-400
               dark:text-neutral-600"
      >
        02 / Capabilities
      </p>

      <h2
        class="text-3xl font-semibold tracking-[-0.04em]
               text-neutral-900 sm:text-4xl dark:text-white"
      >
        What I work with.
      </h2>
    </div>

    <!-- CAPABILITIES -->
    <div class="grid gap-px overflow-hidden rounded-xl
                border border-neutral-200 bg-neutral-200
                sm:grid-cols-2
                dark:border-neutral-800 dark:bg-neutral-800">

      <!-- FRONTEND -->
      <article class="capability">
        <span class="capability-number">01</span>

        <h3>Frontend</h3>

        <p>
          Responsive and maintainable interfaces for modern web products.
        </p>

        <div class="stack">
          <span>TypeScript</span>
          <span>React</span>
          <span>Next.js</span>
          <span>Angular</span>
        </div>
      </article>

      <!-- BACKEND -->
      <article class="capability">
        <span class="capability-number">02</span>

        <h3>Backend & Data</h3>

        <p>
          APIs, business logic, asynchronous workflows and data systems.
        </p>

        <div class="stack">
          <span>NestJS</span>
          <span>FastAPI</span>
          <span>Spring Boot</span>
          <span>PostgreSQL</span>
          <span>MongoDB</span>
          <span>Redis</span>
        </div>
      </article>

      <!-- AI -->
      <article class="capability">
        <span class="capability-number">03</span>

        <h3>AI</h3>

        <p>
          Applied AI integrated into practical product workflows.
        </p>

        <div class="stack">
          <span>LLM</span>
          <span>RAG</span>
          <span>Document AI</span>
          <span>Computer Vision</span>
          <span>Qdrant</span>
        </div>
      </article>

      <!-- DELIVERY -->
      <article class="capability">
        <span class="capability-number">04</span>

        <h3>Delivery</h3>

        <p>
          Reproducible environments and automated software delivery.
        </p>

        <div class="stack">
          <span>Docker</span>
          <span>Git</span>
          <span>GitHub Actions</span>
          <span>Jenkins</span>
          <span>CI/CD</span>
        </div>
      </article>

    </div>
  </div>
</section>

<style>
  .capability {
    background: white;
    padding: 1.75rem;
    transition:
      background-color 250ms ease,
      transform 250ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  :global(.dark) .capability {
    background: rgb(10 10 10);
  }

  .capability:hover {
    background: rgb(250 250 250);
  }

  :global(.dark) .capability:hover {
    background: rgb(15 15 15);
  }

  .capability-number {
    display: block;

    margin-bottom: 2rem;

    font-family: monospace;
    font-size: 9px;

    letter-spacing: 0.2em;

    color: rgb(163 163 163);
  }

  .capability h3 {
    font-size: 1.2rem;
    font-weight: 600;

    letter-spacing: -0.025em;

    color: rgb(23 23 23);
  }

  :global(.dark) .capability h3 {
    color: rgb(245 245 245);
  }

  .capability p {
    max-width: 28rem;

    margin-top: 0.75rem;

    font-size: 0.8rem;
    line-height: 1.6;

    color: rgb(115 115 115);
  }

  .stack {
    display: flex;
    flex-wrap: wrap;

    gap: 0.45rem;

    margin-top: 1.5rem;
  }

  .stack span {
    padding: 0.3rem 0.55rem;

    border: 1px solid rgb(229 229 229);
    border-radius: 0.4rem;

    font-family: monospace;

    font-size: 9px;

    text-transform: uppercase;
    letter-spacing: 0.08em;

    color: rgb(115 115 115);
  }

  :global(.dark) .stack span {
    border-color: rgb(38 38 38);
    color: rgb(163 163 163);
  }
</style>